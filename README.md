# SinotransAI

中外运AI赋能

```
第一步：用户故事拆分
第二步：任务拆分
第三步：架构设计（含目录结构确定）
第四步：接口文档（按任务做）
第五步：数据库设计
第六步：因为有了接口文档，也有了数据库，接着就是逐个实现前端，后端任务（按任务做，使用已有的表结构）
```

提示词：

请执行 #file:战术层-2-任务拆解与估算提示词.md ,输入材料 材料1 为 #file:用户故事_空白提单管理系统_v4.md ,其他材料为默认，请严格遵循RTGO, 每个任务要单独一个小结，描述的清清楚楚。如果是后端任务，就一个接口一个任务。

从 #file:用户故事拆解结果-V1.md 中，解析出接口文档。每条接口要详细的写出 入口参数，还有返回参数。然后把接口文档保存到
docs/接口文档v1.md 目录下。

请给backend加上后端项目所需要的所有配置文件。比如application-dev.yml 或 application.properties，pom.xml.

Comment-Driven Development


实际的简化需求：

可以这么说，先是创建 空白提单的segment. 每段就是 从一个编号到一个编号，一个范围，如果这个范围内有100个编号，那就是创建了 100个 空白提单。。然后每个提单有操作。就是log。就这样的流程。。




使用playwright mcp工具 帮我执行测试用例
1.打开浏览器
2.访问地址：http://localhost:5173/
3.点击按钮 新建空白提单入库
4.等弹出框显示之后, 起始号输入 : 00010019, 结束号输入: 00010021,点击保存
7.判断是否回到了刚才页面，如果有则输出测试通过，如果没有则输出测试不通过
