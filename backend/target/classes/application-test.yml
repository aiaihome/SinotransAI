spring:
  datasource:
    url: jdbc:mysql://192.168.2.50:3306/sinotrans_blankbill_test?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: "7sQRN9#H@uAK"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate.format_sql: false
server:
  port: 18080
  servlet:
    context-path: /
logging:
  level:
    root: WARN
    com.sinotrans: INFO
  file:
    name: logs/sinotrans-blankbill-test.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
