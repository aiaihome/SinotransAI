spring:
  datasource:
    url: jdbc:mysql://192.168.2.50:3306/sinotrans_blankbill?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: "7sQRN9#H@uAK"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true
server:
  port: 8080
  servlet:
    context-path: /
logging:
  level:
    root: INFO
    com.sinotrans: DEBUG
  file:
    name: logs/sinotrans-blankbill-dev.log
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
