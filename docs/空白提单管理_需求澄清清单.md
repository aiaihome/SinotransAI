# 需求澄清清单 · 空白提单管理场景

> **文档版本**：v1.0  
> **创建日期**：2026-03-30  
> **来源文档**：空白提单管理场景案例.docx  
> **编制方**：需求工程师  
> **用途**：供业务方/产品负责人在需求澄清会议上逐条确认，回答后更新本文档

---

## 问题总览

| # | 分类 | 优先级 | 问题简述 | 状态 |
|---|------|--------|---------|------|
| Q01 | 数据定义 | 🔴 高 | 序列号的格式与规则 | 待确认 |
| Q02 | 数据定义 | 🔴 高 | 是否按船司维度隔离管理 | 待确认 |
| Q03 | 状态机 | 🔴 高 | "已归档"状态如何触发 | 待确认 |
| Q04 | 状态机 | 🔴 高 | 回收后序列号状态如何变更 | 待确认 |
| Q05 | 业务规则 | 🔴 高 | 号码段交叉重叠如何处理 | 待确认 |
| Q06 | 业务规则 | 🔴 高 | 作废原因枚举值有哪些 | 待确认 |
| Q07 | 业务规则 | 🔴 高 | 已使用号码段是否允许编辑 | 待确认 |
| Q08 | 查询逻辑 | 🟡 中 | 查询条件必填逻辑 | 待确认 |
| Q09 | 用例边界 | 🟡 中 | UC2与UC3是否合并 | 待确认 |
| Q10 | 用例边界 | 🟡 中 | "导出作废清单"的格式与内容 | 待确认 |
| Q11 | 用例边界 | 🟡 中 | 回收的范围限制 | 待确认 |
| Q12 | 权限与安全 | 🟡 中 | 操作权限控制 | 待确认 |
| Q13 | 性能 | 🟢 低 | 数据量级与性能要求 | 待确认 |
| Q14 | 扩展性 | 🟢 低 | 多港口/多公司场景 | 待确认 |

---

## 一、数据定义类（必须先确认，影响数据库设计）

---

### Q01 🔴 序列号的格式与规则

**背景**：文档中仅提到"序列号起始、结束号段"，未定义格式。

**问题**：

1. 序列号是纯数字，还是包含字母前缀（如 `COSCO-000001`）？
2. 不同船司的序列号格式是否相同？
3. 序列号总长度是固定的吗（如8位、10位）？
4. 起始号和结束号是否要求连续（中间不允许有间断）？
5. 如果是字符串格式，比较大小的规则是什么（字符串排序 vs 数字排序）？

**业务方回答**：
> _（待填写）_

---

### Q02 🔴 是否按船司维度管理

**背景**：文档说"船公司提供空白提单供我司打印"，但入库表单中没有"船司"字段。

**问题**：

1. 不同船司的空白提单序列号是否需要区分管理（防止不同船司的序列号冲突）？
2. 如果需要区分，入库时是否要选择对应船司？
3. 查询时是否需要按船司筛选？

**业务方回答**：
> _（待填写）_

---

## 二、状态机与流转类（影响核心业务逻辑）

---

### Q03 🔴 "已归档"状态如何触发

**背景**：文档声明状态包含：已入库、已使用、**已归档**，但所有功能描述中未出现任何触发"已归档"的操作。

**问题**：

1. "已归档"是什么含义？是该号码段内的提单全部使用完毕后自动归档，还是手动操作？
2. 触发条件是什么（如：已使用数量 = 总数量）？
3. 归档后还能否被查询？能否被回收或作废？
4. 是整个号码段一起归档，还是单张序列号可以单独归档？

**业务方回答**：
> _（待填写）_

---

### Q04 🔴 回收后序列号的状态变更

**背景**：文档功能点8说"回收选中的空白提单序列号，清空该序列号关联的提单号，释放当前序列号可再次打印"，但未说明回收后状态如何变化。

**问题**：

1. 回收后，该序列号的状态变为什么（"已入库"、"已回收"，还是新增一个中间状态）？
2. 回收后的序列号是否立刻可用，还是需要经过某种确认流程？
3. 如果序列号已归档，是否还允许回收？
4. 一次可以批量回收多条吗？（文档说"一个或多个"，但需确认上限）

**业务方回答**：
> _（待填写）_

---

### Q05 🔴 号码段重叠或交叉的处理规则

**背景**：UC1 异常流写道"序列号的起始、结束号段已存在不允许保存"，但"已存在"的判断规则不明确。

**问题**：

1. "已存在"的判断逻辑是：**完全相同**，还是**部分交叉**也算？
   - 示例：已录入 100~200，新录入 150~250 → 是否报错？
   - 示例：已录入 100~200，新录入 201~300 → 是否允许（紧邻但不重叠）？
2. 如果已有的号码段被作废，它占用的序列号区间是否释放，允许重新录入？

**业务方回答**：
> _（待填写）_

---

## 三、业务规则类（影响功能实现细节）

---

### Q06 🔴 作废原因的枚举值

**背景**：功能点7"子页面-作废"提到"选择作废原因"，但原因选项未定义。

**问题**：

1. 作废原因有哪些选项？（如：打印错误、污损、丢失、船司回收…）
2. 是否支持自定义填写原因？
3. 作废原因是否影响后续的"导出作废清单"内容？
4. 作废操作是否需要二次确认弹窗？

**业务方回答**：
> _（待填写）_

---

### Q07 🔴 已使用号码段是否允许编辑

**背景**：功能点4支持"编辑"，但未说明限制条件。

**问题**：

1. 如果一个号码段中已有部分序列号被使用，是否还允许修改起始/结束号码？
2. 如果修改后的号码段范围**缩小**，已使用的序列号超出范围了怎么办？
3. 哪些字段允许编辑？（备注？号码段？入库日期？）
4. 编辑是否需要记录修改历史/操作日志？

**业务方回答**：
> _（待填写）_

---

### Q08 🟡 查询条件的必填逻辑

**背景**：存在矛盾——UC2 异常流说"提单号、使用日期、入库日期三选一必填"，但原型要素表中这三个字段的"是否必填"均标注为 N（非必填）。

**问题**：

1. 以哪个为准？查询时是否有最低条件限制？
2. 如果不输入任何条件直接点查询，是否展示所有记录（可能数据量极大）？
3. 是否需要设置分页，或者对无条件查询做限制（如提示"请至少输入一个查询条件"）？

**业务方回答**：
> _（待填写）_

---

### Q11 🟡 回收的范围限制

**背景**：文档说可以回收"已关联提单号的序列号"，但未说明限制。

**问题**：

1. 只有"已使用"状态的序列号才能被回收吗？"已归档"的可以回收吗？
2. 如果该提单已经放单（正本提单已交给客户），是否还允许回收？回收是否有权限限制？
3. 回收是否需要说明原因（类似作废原因）？
4. 回收后是否通知放单岗/客户？

**业务方回答**：
> _（待填写）_

---

## 四、用例边界类（影响功能结构设计）

---

### Q09 🟡 UC2 与 UC3 是否合并

**背景**：UC2「通过提单号查询序列号详情」与 UC3「通过提单号查询空白提单信息」需求描述完全相同，正常事件流前3步逐字一致，UC3 仅在第4步增加了回收操作。

**问题**：

1. 这两个用例是否可以合并为一个"查询序列号详情"用例，将"回收"作为操作按钮？
2. 还是两者在业务上有本质区别，需要分开入口？

**建议**：合并为一个用例，降低系统复杂度和用户认知成本。

**业务方回答**：
> _（待填写）_

---

### Q10 🟡 导出作废清单的格式与内容

**背景**：功能点6"导出作废清单"描述极简，仅说"用于与船司线下交接纸质单据"。

**问题**：

1. 导出格式是 Excel 还是 PDF？船司是否有固定格式要求？
2. 导出内容包含哪些字段？（序列号、作废原因、作废日期、作废操作人…）
3. 是导出当前筛选结果中的作废数据，还是导出选中行的作废数据？
4. 是否需要签名栏/盖章位置（用于线下交接）？

**业务方回答**：
> _（待填写）_

---

## 五、权限与安全类

---

### Q12 🟡 操作权限控制

**背景**：提单管理是敏感的财务/物流操作，文档仅说参与者是"制单岗"，无权限细节。

**问题**：

1. 除"制单岗"外，其他岗位（如主管、审核岗）是否有只读权限？
2. 哪些操作需要额外审核？（如：作废、回收是否需要主管审批？）
3. 所有操作是否需要记录操作日志（操作人、时间、操作内容、修改前后值）？
4. 是否有防止重复提交的要求（如：连续点击"保存"是否会产生重复入库记录）？

**业务方回答**：
> _（待填写）_

---

## 六、性能与扩展性

---

### Q13 🟢 数据量级与性能要求

**问题**：

1. 预计每年入库的空白提单序列号段有多少条？序列号总张数量级（万级/百万级）？
2. 查询响应时间要求（如：< 3秒）？
3. 是否有并发操作的场景（多个制单岗同时操作）？

**业务方回答**：
> _（待填写）_

---

### Q14 🟢 多港口/多公司场景扩展

**问题**：

1. 系统是单公司使用，还是未来需要支持集团多子公司？
2. 不同港口的提单管理是否相互独立？
3. 是否需要多语言支持（如中英双语提单）？

**业务方回答**：
> _（待填写）_

---

## 附录：问题优先级说明

| 优先级 | 标识 | 含义 | 建议处理时机 |
|--------|------|------|-------------|
| 高 | 🔴 | 影响核心数据结构或状态机，不确认无法开始设计 | 需求澄清会前必须解决 |
| 中 | 🟡 | 影响功能边界或交互细节，可以在设计阶段确认 | 设计评审前解决 |
| 低 | 🟢 | 影响非功能需求，可在架构设计时确认 | 开发启动前解决 |

---

*本文档由需求工程师自动分析生成，请业务方逐条填写"业务方回答"后返回。*
